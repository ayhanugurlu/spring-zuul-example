package com.au.example.proxy.repository;

import com.au.example.proxy.model.ProxyLog;
import org.springframework.data.repository.CrudRepository;

public interface ProxyLogRepository extends CrudRepository<ProxyLog,Long> {
}
